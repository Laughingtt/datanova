如果有配置数据源，那就默认选用已配置的数据源。

在 Schema 标注模块里面也加入 AI 自动化标注，将字段或数据推荐到字段类型表进行标注。

关于指标管理有以下几点建议：
1. 列表里的英文请改成中文，例如 ID、Magic Name、Display Name 这些现在都还是英文。
2. 修复指标管理里的 AI 推荐指标，点击后没有效果。
3. 固定数据字典。
4. 指标管理里应该能看到数据字典里已发布的指标。

另外，请确认业务逻辑：是不是只有已发布的指标才能被搜索并真正应用到？